# The Pinky Toe Website - Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# AIRTABLE CONFIGURATION (OPTIONAL - Development Only)
# ============================================================================
# The app works with sample data if these are not provided
# These credentials are ONLY used in development mode and are never exposed to the client

# Your Airtable API key - get this from https://airtable.com/account
# IMPORTANT: Keep this secret! Never commit this to Git
AIRTABLE_API_KEY=your_airtable_api_key_here

# Your Airtable base ID - found in your Airtable API docs
AIRTABLE_BASE_ID=your_airtable_base_id_here

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

# Application environment
NODE_ENV=development

# Server port (default: 5000, auto-increments if port is busy)
PORT=5000

# Server host (default: 0.0.0.0)
HOST=0.0.0.0

# ============================================================================
# SECURITY NOTES
# ============================================================================
# - This .env file is excluded from Git via .gitignore
# - Airtable credentials are only used server-side during development
# - GitHub Pages deployment uses static cached data (no API keys needed)
# - Never expose API keys in client-side code